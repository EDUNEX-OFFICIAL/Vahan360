# Prisma CLI (migrate, studio, validate) — same Postgres instance as backend is OK.
# New tables live in schema `ingest` (see prisma/schema.prisma); do not commit real credentials.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/DBNAME"
